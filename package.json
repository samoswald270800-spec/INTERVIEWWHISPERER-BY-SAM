{
  "name": "interview-whisperer",
  "version": "2.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd public && npm install && npm run build",
    "postinstall": "cd public && npm install && npm run build && cd ../admin && npm install && npm run build",
    "dev": "node server.js",
    "deploy": "npm run build && npm start"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^2.0.50",
    "@supabase/supabase-js": "^2.86.0",
    "ai": "^5.0.104",
    "connect-redis": "^8.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-session": "^1.17.3",
    "node-fetch": "^3.3.2",
    "openai": "^4.0.0",
    "redis": "^4.6.12"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
