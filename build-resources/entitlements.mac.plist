<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow microphone access (live interview listening) -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- Allow camera access (optional video support) -->
    <key>com.apple.security.device.camera</key>
    <true/>

    <!-- Allow screen recording / display capture (screen analysis) -->
    <key>com.apple.security.device.screen-capture</key>
    <true/>

    <!-- Allow outgoing network connections (API calls to OpenAI, Anthropic, Railway) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Allow loading unsigned in-process code (Electron renderer) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow DYLD env variables (needed for Electron) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>

    <!-- Disable library validation (needed for Electron native modules) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
